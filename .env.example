# Google Calendar OAuth 配置
# 如需使用 Google Calendar 同步功能，请按以下步骤操作：
# 
# 1. 在 Google Cloud Console 创建 OAuth 2.0 客户端（类型选择 "Web application"）
# 2. 添加授权重定向 URI（详见 docs/GOOGLE_CALENDAR_OAUTH_SETUP.md）：
#    - http://localhost:8888/oauth2callback
#    - http://localhost:8889/oauth2callback
#    - http://localhost:8890/oauth2callback
#    - http://localhost:9999/oauth2callback
#    - http://localhost:3000/oauth2callback
# 3. 复制客户端 ID 和密钥，填写到下面
# 4. 将此文件重命名为 .env
# 5. 打包时，.env 文件会自动包含到应用中
#
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
